version = getSubprojectVersion(project)

moduleDependencies(project, ['fabric-api-base'])

jar {
    manifest {
        attributes(
                'Automatic-Module-Name': 'net.fabric.api.keybinding'
        )
    }
}